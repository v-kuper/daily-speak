# Public browser-visible API origin. No trailing slash is required.
PUBLIC_API_BASE_URL=http://localhost:3219
# Optional canonical web origin. LAN HTTPS deploys use it to redirect direct HTTP access.
PUBLIC_WEB_BASE_URL=
